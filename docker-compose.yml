services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      CORS_ORIGINS: "*"
      CARBON_FACTOR: "0.47"
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
